package de.kesselops.shared.model;

/**
 * User roles in the KesselOps system.
 * Defines access levels from Owner (full access) to Trainee (limited access).
 */
public enum Role {
    OWNER,
    MANAGER,
    STAFF,
    TRAINEE
}
