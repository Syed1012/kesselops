services:
  postgres:
    image: postgres:16-alpine
    container_name: kesselops-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: kesselops
      POSTGRES_USER: kesselops
      POSTGRES_PASSWORD: kesselops_dev
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kesselops"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: kesselops-redis
    ports:
      - "6379:6379"

  minio:
    image: minio/minio:latest
    container_name: kesselops-minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data

volumes:
  pgdata:
  minio_data:
